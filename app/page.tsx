import Link from 'next/link';
import {ArrowUpRight,ArrowRight,Layers,FlaskConical} from 'lucide-react';
import {LabHeader} from './simulator-nav';
import {simulators} from '@/lib/simulator-catalog';
export default function Home(){return <main className="portal"><LabHeader active="home"/><div className="portal-content"><div className="portal-heading"><div><span className="portal-kicker">SOMA / SIMULATORS</span><h1>어떤 실험을 시작할까요?</h1><p>전신에서 국소 조직까지. 목적에 맞는 시뮬레이터를 선택하세요.</p></div><span className="portal-count">02 <small>WORKSPACES</small></span></div><section className="simulator-grid" aria-label="시뮬레이터 목록">{simulators.map(sim=><article className="simulator-card" key={sim.id}><Link href={sim.href} className="simulator-thumbnail" aria-label={sim.action}><img src={sim.image} alt={sim.alt} width="800" height="500"/><span className="simulator-number">{sim.number}</span><span className="simulator-image-label">{sim.en}</span><span className="simulator-open-icon"><ArrowUpRight size={24}/></span></Link><div className="simulator-card-body"><span className="simulator-status"><i className="status-dot"/>{sim.status}</span><h2><Link href={sim.href}>{sim.title}</Link></h2><p>{sim.description}</p><ul>{sim.features.map(feature=><li key={feature}><Layers size={15}/>{feature}</li>)}</ul><Link href={sim.href} className="simulator-launch">{sim.action}<ArrowRight size={18}/></Link></div></article>)}</section><section className="portal-next"><FlaskConical size={25}/><div><span className="portal-kicker">NEXT / HAND & WRIST</span><h2>센서 배열에서 연속 혈압 추정까지</h2><p>손·손목 정밀 해부학, MVDR, ECG-PAT·HR 연동과 독립 검증을 다음 확장 단계로 설계합니다.</p></div><Link href="/research">설계 및 진행 기록<ArrowUpRight size={18}/></Link></section><footer className="portal-footer"><span>가상 실험용 연구 모델 · 합성 결과는 임상 정확도를 뜻하지 않습니다.</span><span>Z-Anatomy · BodyParts3D · CMU</span></footer></div></main>}
